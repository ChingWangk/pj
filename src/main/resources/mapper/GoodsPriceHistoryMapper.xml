<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="your.package.name.GoodsPriceHistoryMapper">
    <resultMap id="goodsPriceHistoryMap" type="your.package.name.GoodsPriceHistory">
        <id property="id" column="id" />
        <result property="goodsId" column="goods_id" />
        <result property="price" column="price" />
        <result property="timestamp" column="timestamp" />
    </resultMap>

    <select id="getGoodsPriceHistory" resultMap="goodsPriceHistoryMap">
        SELECT id, goods_id, price, timestamp
        FROM goods_price_history
        WHERE goods_id = #{goodsId}
        AND timestamp BETWEEN #{startTime} AND #{endTime}
        ORDER BY timestamp ASC;
    </select>

    <!-- Other SQL queries... -->
</mapper>